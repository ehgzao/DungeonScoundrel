# Security headers (CSP, COOP, COEP, X-*, Permissions-Policy, cache-control)
# live in ONE place: netlify.toml ([[headers]] for = "/*").
#
# This file previously duplicated the Content-Security-Policy / COOP / COEP,
# but netlify.toml takes precedence over _headers for conflicting paths, so the
# copy here was dead AND divergent (looser img-src, missing connect-src origins
# Firebase needs). Removed to avoid two sources of truth. Add Netlify header
# rules here ONLY if they are not already covered by netlify.toml.
